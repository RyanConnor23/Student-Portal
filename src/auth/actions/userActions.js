export const loginUser = (credentias, navigate, setFieldError, setSubmitting) => {
//Make checks and stuff
const user = {
    name: "George",
    "email":"gee@gmail.com"
}

const status = true;
if (status === true) {
    //Alow access and redirect
} else {
    //return error to the user
}
}
export const signupUser = (credentias, navigate, setFieldError, setSubmitting) => {

}

export const logoutUser = () => {

}